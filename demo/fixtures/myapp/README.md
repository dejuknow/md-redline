# Atlas Mobile

Companion app for the Atlas project workspace. See `docs/` for the
architecture notes and `prd.md` for the current product spec.

## Getting started

```bash
npm install
npm run dev
```
