/**
 * Vertical separator for toolbars and tab bars.
 * Use this component instead of inline divs to keep separator styling consistent.
 */
export function Separator() {
  return <div className="h-4 w-px bg-border mx-1 shrink-0" />;
}
